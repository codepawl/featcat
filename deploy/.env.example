# Copy to deploy/.env and fill in if building behind corporate proxy (e.g., FPT labtest).
# Local dev machines with direct internet should leave this unset.

# HTTP_PROXY=http://proxy.hcm.fpt.vn:80
# HTTPS_PROXY=http://proxy.hcm.fpt.vn:80

# Labtest uses a different LLM model filename (with .gguf suffix)
# FEATCAT_LLM_MODEL=gemma-4-E2B-it-Q4_K_M.gguf

# Data directory on host containing Parquet files
# DATA_DIR=/path/to/your/data

# Port to expose featcat (default 8000)
# FEATCAT_PORT=8000

# Optional: auth token for API access
# SERVER_AUTH=
